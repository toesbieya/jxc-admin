<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.toesbieya.jxc.mapper.SysDepartmentMapper">
    <select id="get" resultType="cn.toesbieya.jxc.model.entity.SysDepartment">
        select *
        from sys_department
        where status = 1
    </select>
    <select id="getAll" resultType="cn.toesbieya.jxc.model.entity.SysDepartment">
        select *
        from sys_department
    </select>
    <insert id="add" parameterType="cn.toesbieya.jxc.model.entity.SysDepartment">
        insert into sys_department (pid, name, status)
        VALUES (#{pid}, #{name}, #{status});
    </insert>
    <update id="update" parameterType="cn.toesbieya.jxc.model.entity.SysDepartment">
        update sys_department
        set pid=#{pid},
        name=#{name},
        status=#{status}
        where id = #{id}
    </update>
    <delete id="del">
        delete
        from sys_department
        where id = #{id}
    </delete>
    <select id="nameExist" resultType="java.lang.Boolean">
        select if(count(0) > 0, 1, 0)
        from sys_department
        where name = #{name}
        and pid = #{pid}
        <if test="id!=null">
            and id!=#{id}
        </if>
    </select>
</mapper>